let screen = document.querySelector(".screen");let numBtn = document.querySelectorAll(".btn").length;for (let i = 0; i < numBtn; i++) {  document.querySelectorAll(".btn")[i].addEventListener("click", function () {    screen.value += this.value;  });};let opr = ["+", "/", "*", "-"];let numberOfBtn = document.querySelectorAll(".operator").length;for (let i = 0; i < numberOfBtn; i++) {  document.querySelectorAll(".operator")[i].addEventListener("click", function () {    if (this.value !== screen.value.slice(-1)) {      if (!opr.includes(screen.value.slice(-1))) {        screen.value += this.value;      } else {        changeOpr(this.value)      }    }  });};function dotMinus(argument) {  if (argument === '.') screen.value += argument;  if (argument === '-') screen.value += argument;};function changeOpr(argument) {  if(argument === "(" || argument === ")"){screen.value += argument}  else if (opr.includes(screen.value.slice(-1)) && argument !== "." && argument !== "-" && !opr.includes(screen.value.slice(-2, -1))) {    screen.value = screen.value.slice(0, -1);    screen.value += argument;  }  else {dotMinus(argument)}};function calculation() {  try {    if (!opr.includes(screen.value.slice(-1))) {      screen.value = Function('return (' + screen.value + ')')().toFixed(2).replace(".00", "")    } else {      let sliceBLC = screen.value.slice(0, -1);      screen.value = Function('return (' + sliceBLC + ')')().toFixed(2).replace(".00", "")    }  } catch (error) {    screen.value = ""  }};document.getElementById('clearAll').addEventListener('click', () => screen.value = "");document.getElementById('clear1').addEventListener('click', () => screen.value = screen.value.slice(0, -1));document.querySelector(".equalOperator").addEventListener('click', () => calculation());document.addEventListener('keydown', function (event) {  let oprText = ["+", "/", "*", "(", ")"];  let oprText1 = ["+", "/", "*", "(", ")", ".", "-"];  if (event.key !== screen.value.slice(-1)) {    if (oprText.includes(event.key) && !opr.includes(screen.value.slice(-1))) screen.value += event.key;    else if (oprText1.includes(event.key)) changeOpr(event.key);  };  let numText = ["7", "8", "9", "4", "5", "6", "1", "2", "3", "0"];  if (numText.includes(event.key)) screen.value += event.key;  if (event.key === "Backspace") screen.value = screen.value.slice(0, -1);  if (event.key === "Enter") calculation();});